name: Rust

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run tests
      run: cargo test --verbose --all --release
  release_build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]
    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: cargo build --verbose --all --release
    - name: Tar files
      run: tar -cvf ${{ matrix.os }}_build.tar `ls target/release/poodle target/release/poodle.exe`
    - name: Archive release binary
      uses: actions/upload-artifact@v2
      with:
        name: build
        path: |
          target/release/poodle
          target/release/poodle.exe
        retention-days: 3
